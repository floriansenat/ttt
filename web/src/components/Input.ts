import { styled } from "@/libs/stitches";

export const Input = styled("input", {
  backgroundColor: "$white",
  border: "none",
  display: "block",
  width: "100%",
  borderRadius: "$md",
  height: "48px",
  fontSize: "1rem",
  padding: "0 12px",
});
